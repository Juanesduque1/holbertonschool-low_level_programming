#ifndef HEADER_H
#define HEADER_H

/* Libraries */
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

/* Prototypes */
int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);

#endif
