#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* print_alphabet - print alphabet in lowercase
*/

void print_alphabet(void);
{
char ch;

while (ch <= 'z')
{
putchar(ch);
}
putchar('\n');

return (0);
}
